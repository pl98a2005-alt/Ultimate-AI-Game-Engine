name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev curl jq
          pip install --user --upgrade buildozer cython virtualenv

      - name: Sovereign Path Injection & Build
        run: |
          # 1. تحديد المسارات الأصلية في السيرفر
          REAL_SDK="/usr/local/lib/android/sdk"
          REAL_NDK="/usr/local/lib/android/sdk/ndk/27.3.13750724"
          
          # 2. 👑 الخدعة الملكية: إنشاء المجلدات التي يتوقعها Buildozer وربطها بالأدوات الحقيقية
          sudo mkdir -p $REAL_SDK/tools/bin
          # ربط sdkmanager الحديث بالمكان القديم الذي يبحث عنه المحرك
          sudo ln -sf $REAL_SDK/cmdline-tools/latest/bin/sdkmanager $REAL_SDK/tools/bin/sdkmanager
          
          # 3. قبول التراخيص باستخدام المسار الحقيقي
          yes | $REAL_SDK/cmdline-tools/latest/bin/sdkmanager --sdk_root=$REAL_SDK --licenses || true
          
          # 4. تنظيف البيئة وبدء البناء السيادي
          rm -rf .buildozer
          buildozer -v android debug
        env:
          ANDROID_HOME: /usr/local/lib/android/sdk
          ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
          ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/27.3.13750724
        continue-on-error: false

      - name: Deliver to Telegram
        if: success()
        run: |
          APK_PATH=$(find bin/ -name "*.apk" | head -n 1)
          # رفع الملف والحصول على رابط QR
          DOWNLOAD_URL=$(curl -F "file=@$APK_PATH" https://file.io | jq -r '.link')
          QR_URL="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=$DOWNLOAD_URL"
          
          # إرسال الملف والـ QR إلى تليجرام
          curl -v -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" -F "document=@$APK_PATH" \
               -F "caption=👑 السيادة تحققت! المصنع بين يديك." \
               https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendPhoto" \
               -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" -d photo="$QR_URL" -d caption="📸 كود التثبيت السريع"
