name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 👑 المرحلة 1: التحميل الفولاذي (خارج Buildozer)
      - name: Pre-Download NDK & Fonts
        run: |
          # تحميل الخط العربي للقضاء على المربعات
          curl -L -o Cairo-Bold.ttf https://github.com/google/fonts/raw/main/ofl/cairo/Cairo-Bold.ttf
          
          # تحميل الـ NDK باستخدام wget مع خاصية إعادة المحاولة تلقائياً
          mkdir -p $HOME/android_tools
          wget -c --tries=10 https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
          unzip -q android-ndk-r25b-linux.zip -d $HOME/android_tools
          echo "NDK_HOME_PATH=$HOME/android_tools/android-ndk-r25b" >> $GITHUB_ENV

      - name: Install Core Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev openjdk-17-jdk
          pip install --user --upgrade "buildozer>=1.5.0" "Cython<3.0" virtualenv

      # 👑 المرحلة 2: تشغيل المصنع السيادي
      - name: Run Buildozer
        run: |
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          export PATH=$PATH:$HOME/.local/bin
          
          # حقن المسارات في الـ Spec لمنع التحميل الفاشل
          sed -i "s|^#\? \?android.ndk_path =.*|android.ndk_path = ${{ env.NDK_HOME_PATH }}|" buildozer.spec
          sed -i "s|^#\? \?android.accept_sdk_license =.*|android.accept_sdk_license = True|" buildozer.spec
          
          # تشغيل البناء مع عرض التفاصيل الكاملة
          buildozer -v android debug
