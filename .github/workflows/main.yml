name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev curl jq
          pip install --user --upgrade buildozer cython virtualenv

      - name: Isolated Environment Reconstruction
        run: |
          # 1. تنظيف شامل للمشروع
          rm -rf .buildozer bin
          
          # 2. إنشاء هيكل أندرويد خاص بنا تماماً بعيداً عن النظام
          mkdir -p $GITHUB_WORKSPACE/android_storage/cmdline-tools
          curl -o sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q sdk.zip -d $GITHUB_WORKSPACE/android_storage/cmdline-tools
          mv $GITHUB_WORKSPACE/android_storage/cmdline-tools/cmdline-tools $GITHUB_WORKSPACE/android_storage/cmdline-tools/latest
          
          # 3. إجبار Buildozer على استخدام هذا المسار عبر ملف الإعدادات مباشرة
          # سنقوم بحقن المسار الجديد داخل buildozer.spec برمجياً لضمان عدم الخطأ
          sed -i "s|# android.sdk_path =|android.sdk_path = $GITHUB_WORKSPACE/android_storage|g" buildozer.spec
          
          # 4. تعريف المسارات الضرورية للبناء
          export PATH=$PATH:$GITHUB_WORKSPACE/android_storage/cmdline-tools/latest/bin
          
          # 5. قبول التراخيص في البيئة الجديدة
          yes | sdkmanager --sdk_root=$GITHUB_WORKSPACE/android_storage --licenses || true
          
          # 6. تشغيل البناء مع تجاهل مسارات السيرفر
          buildozer -v android debug
        env:
          # تصفير متغيرات النظام لإجبار المحرك على القراءة من المجلد الجديد
          ANDROID_HOME: ""
          ANDROID_SDK_ROOT: ""
          ANDROID_NDK_HOME: ""
        continue-on-error: false

      - name: Deliver to Telegram
        if: success()
        run: |
          APK_PATH=$(find bin/ -name "*.apk" | head -n 1)
          DOWNLOAD_URL=$(curl -F "file=@$APK_PATH" https://file.io | jq -r '.link')
          QR_URL="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=$DOWNLOAD_URL"
          
          curl -v -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" -F "document=@$APK_PATH" \
               -F "caption=👑 تمت السيادة المطلقة! المصنع جاهز." \
               https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendPhoto" \
               -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" -d photo="$QR_URL" -d caption="📸 مسح للتثبيت السريع"
