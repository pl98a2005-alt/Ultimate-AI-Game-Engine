name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev curl jq
          pip install --user --upgrade buildozer cython virtualenv

      - name: Physical SDK Override & Build
        run: |
          # 1. تنظيف شامل
          rm -rf .buildozer bin
          
          # 2. 👑 الضربة القاضية: تدمير المسار القديم واستبداله
          # سنقوم بإنشاء هيكلية الأندرويد داخل مجلد المشروع أولاً
          mkdir -p $GITHUB_WORKSPACE/new_sdk/cmdline-tools
          curl -o sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q sdk.zip -d $GITHUB_WORKSPACE/new_sdk/cmdline-tools
          mv $GITHUB_WORKSPACE/new_sdk/cmdline-tools/cmdline-tools $GITHUB_WORKSPACE/new_sdk/cmdline-tools/latest
          
          # إنشاء مجلد bin الوهمي الذي يبحث عنه بيلدوزر
          mkdir -p $GITHUB_WORKSPACE/new_sdk/tools/bin
          ln -s $GITHUB_WORKSPACE/new_sdk/cmdline-tools/latest/bin/sdkmanager $GITHUB_WORKSPACE/new_sdk/tools/bin/sdkmanager
          
          # 3. حقن المسار الجديد في ملف الإعدادات برمجياً لضمان السيطرة
          sed -i "s|# android.sdk_path =|android.sdk_path = $GITHUB_WORKSPACE/new_sdk|g" buildozer.spec
          sed -i "s|android.accept_sdk_license = False|android.accept_sdk_license = True|g" buildozer.spec
          
          # 4. قبول التراخيص يدوياً قبل الانطلاق
          export PATH=$PATH:$GITHUB_WORKSPACE/new_sdk/cmdline-tools/latest/bin
          yes | sdkmanager --sdk_root=$GITHUB_WORKSPACE/new_sdk --licenses || true
          
          # 5. تشغيل البناء مع عزل متغيرات النظام تماماً
          env ANDROID_HOME=$GITHUB_WORKSPACE/new_sdk ANDROID_SDK_ROOT=$GITHUB_WORKSPACE/new_sdk buildozer -v android debug
        continue-on-error: false

      - name: Deliver to Telegram
        if: success()
        run: |
          APK_PATH=$(find bin/ -name "*.apk" | head -n 1)
          DOWNLOAD_URL=$(curl -F "file=@$APK_PATH" https://file.io | jq -r '.link')
          QR_URL="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=$DOWNLOAD_URL"
          
          curl -v -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" -F "document=@$APK_PATH" \
               -F "caption=👑 تمت السيادة! تطبيقك جاهز للاستخدام." \
               https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendPhoto" \
               -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" -d photo="$QR_URL" -d caption="📸 مسح للتثبيت السريع"
