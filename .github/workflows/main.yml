      - name: Sovereign Tools Alignment
        run: |
          rm -rf .buildozer bin
          # 1. تثبيت الـ SDK في المسار الصحيح
          mkdir -p $HOME/android_sdk/cmdline-tools
          curl -L -o sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q sdk.zip -d $HOME/android_sdk/cmdline-tools
          mv $HOME/android_sdk/cmdline-tools/cmdline-tools $HOME/android_sdk/cmdline-tools/latest
          
          # 2. 👑 جسر السيادة: ربط المسارات لإرضاء Buildozer
          # سنقوم بإنشاء المجلد الذي يبحث عنه البرنامج ونربطه بالملف الحقيقي
          mkdir -p $HOME/android_sdk/tools/bin
          ln -s $HOME/android_sdk/cmdline-tools/latest/bin/sdkmanager $HOME/android_sdk/tools/bin/sdkmanager
          ln -s $HOME/android_sdk/cmdline-tools/latest/bin/avdmanager $HOME/android_sdk/tools/bin/avdmanager
          
          # 3. تفعيل المسارات في بيئة النظام
          export PATH=$PATH:$HOME/android_sdk/cmdline-tools/latest/bin
          
          # 4. قبول التراخيص وحقنها
          mkdir -p $HOME/android_sdk/licenses
          printf "8933bad161af4178b1185d1a37fbf41ea5269c55\nd56f5187479451eabf01fb7437c302a91464e303" > $HOME/android_sdk/licenses/android-sdk-license
          yes | sdkmanager --sdk_root=$HOME/android_sdk --licenses || true
          
          # 5. تثبيت المنصات المطلوبة
          sdkmanager --sdk_root=$HOME/android_sdk "platforms;android-33" "build-tools;33.0.0" "ndk;25.2.9519653"

          # 6. حقن المسارات في spec
          sed -i 's|^#\? \?android.sdk_path =.*|android.sdk_path = /home/runner/android_sdk|' buildozer.spec
          sed -i 's|^#\? \?android.ndk_path =.*|android.ndk_path = /home/runner/android_sdk/ndk/25.2.9519653|' buildozer.spec
          
          # 7. الانطلاق
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          buildozer -v android debug
