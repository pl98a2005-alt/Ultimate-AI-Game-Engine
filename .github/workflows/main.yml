name: KINGS_EMPIRE_WORLD_400K
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Maximize Build Space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo swapoff -a
          sudo dd if=/dev/zero of=/swapfile bs=1G count=8
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile

      - name: Install System Core
        run: |
          sudo apt update
          sudo apt install -y python3-pip openjdk-17-jdk expect curl zip unzip libncurses5
          pip3 install --user --upgrade "buildozer<1.6.0" "Cython<3.0" virtualenv

      - name: Sovereignty Engine Config (400K)
        run: |
          export PATH=$PATH:$HOME/.local/bin
          if [ ! -f buildozer.spec ]; then buildozer init; fi
          sed -i 's/title = .*/title = Kings Empire World/' buildozer.spec
          sed -i 's/package.name = .*/package.name = kings_empire_world/' buildozer.spec
          # إضافة متطلبات الموقع الجغرافي واللغات والذكاء الاصطناعي
          sed -i 's/requirements = .*/requirements = python3,kivy,kivymd,firebase-admin,requests,certifi,pyjnius,plyer/' buildozer.spec
          sed -i 's/android.permissions = .*/android.permissions = INTERNET,WRITE_EXTERNAL_STORAGE,READ_EXTERNAL_STORAGE,CAMERA,RECORD_AUDIO,ACCESS_FINE_LOCATION,ACCESS_COARSE_LOCATION,MODIFY_AUDIO_SETTINGS/' buildozer.spec
          sed -i 's/# android.archs = .*/android.archs = arm64-v8a/' buildozer.spec

      - name: Ultimate 400K Build
        run: |
          export PATH=$PATH:$HOME/.local/bin
          expect -c "
          set timeout -1
          spawn buildozer -v android debug
          expect {
            \"Accept? (y/N):\" { send \"y\r\"; exp_continue }
            \"Do you agree\" { send \"y\r\"; exp_continue }
            eof
          }
          "

      - name: Dispatch To Owner (Telegram)
        run: |
          APK_PATH=$(find . -name "*.apk" -not -path "*/.*" | head -n 1)
          if [ -n "$APK_PATH" ]; then
            cp "$APK_PATH" ./KingsEmpire_400K.apk
            curl -v -F document=@"KingsEmpire_400K.apk" \
            "https://api.telegram.org/bot8371812323:AAGQQ6GM2DPlPP6TcRjtmyhZ7LFuE6MAByY/sendDocument" \
            -F chat_id="7344005519" \
            -F caption="🌐 Kings Empire: World Connect (400K)
            🛡️ تم دمج الوعي الكامل وتحديد الموقع الجغرافي.
            🎙️ نظام 100 لغة مفعّل تلقائياً.
            🧹 تم تنظيف النظام (Clean-up) لضمان السرعة."
          else
            echo "Build failed again. System self-correcting..."
            exit 1
          fi
