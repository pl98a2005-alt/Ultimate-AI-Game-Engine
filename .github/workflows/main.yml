name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 👑 خطوة الحقن اليدوي: تحميل الـ NDK والخطوط بأدوات النظام المستقرة
      - name: Pre-Download Tools (Avoid Buildozer Crash)
        run: |
          # 1. تحميل الخط العربي للقضاء على المربعات
          curl -L -o Cairo-Bold.ttf https://github.com/google/fonts/raw/main/ofl/cairo/Cairo-Bold.ttf
          
          # 2. تحميل الـ NDK يدوياً (أكثر استقراراً بـ 10 أضعاف)
          mkdir -p $HOME/android_tools
          wget -c --tries=15 https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
          unzip -q android-ndk-r25b-linux.zip -d $HOME/android_tools
          echo "NDK_HOME_PATH=$HOME/android_tools/android-ndk-r25b" >> $GITHUB_ENV

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev openjdk-17-jdk
          pip install --user --upgrade "buildozer>=1.5.0" "Cython<3.0" virtualenv

      - name: Sovereign Execution (No-Download Mode)
        run: |
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          export PATH=$PATH:$HOME/.local/bin
          
          # 3. إعداد الـ SDK يدوياً لتجنب توقف Buildozer
          export SDK_DIR=$HOME/sovereign_sdk
          mkdir -p $SDK_DIR/cmdline-tools
          curl -L -o sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q sdk.zip -d $SDK_DIR/cmdline-tools
          mv $SDK_DIR/cmdline-tools/cmdline-tools $SDK_DIR/cmdline-tools/latest
          
          # 4. حقن المسارات الجاهزة في ملف الإعدادات
          sed -i "s|^#\? \?android.sdk_path =.*|android.sdk_path = $SDK_DIR|" buildozer.spec
          sed -i "s|^#\? \?android.ndk_path =.*|android.ndk_path = ${{ env.NDK_HOME_PATH }}|" buildozer.spec
          sed -i "s|^#\? \?android.accept_sdk_license =.*|android.accept_sdk_license = True|" buildozer.spec
          sed -i "s|^#\? \?android.skip_update =.*|android.skip_update = True|" buildozer.spec
          
          # انطلاق البناء! لن يحاول تحميل NDK الآن لأنه سيجده جاهزاً
          buildozer -v android debug

      - name: Upload Final APK
        uses: actions/upload-artifact@v4
        with:
          name: Sovereign-Success-Build
          path: bin/*.apk
