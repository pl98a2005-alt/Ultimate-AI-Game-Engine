name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Critical Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev curl jq
          pip install --user --upgrade buildozer cython virtualenv

      - name: Force System Override
        run: |
          # 1. تنظيف شامل
          rm -rf .buildozer bin
          
          # 2. 👑 عملية "الاستحواذ": إصلاح مسار النظام المعطل
          # سنقوم بوضع الأدوات داخل مسار النظام الذي يبحث عنه بيلدوزر رغماً عنه
          sudo mkdir -p /usr/local/lib/android/sdk/tools/bin
          curl -L -o sdk_tools.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          sudo unzip -q sdk_tools.zip -d /tmp/sdk
          sudo mv /tmp/sdk/cmdline-tools/* /usr/local/lib/android/sdk/tools/ || true
          
          # 3. ربط الأداة المفقودة (sdkmanager) بالمكان الذي يطلبه الخطأ
          sudo ln -sf /usr/local/lib/android/sdk/tools/bin/sdkmanager /usr/local/lib/android/sdk/bin/sdkmanager || true
          
          # 4. قبول التراخيص لعام 2026
          export PATH=$PATH:/usr/local/lib/android/sdk/tools/bin
          yes | sudo /usr/local/lib/android/sdk/tools/bin/sdkmanager --sdk_root=/usr/local/lib/android/sdk --licenses || true
          
          # 5. تشغيل بناء السيادة
          buildozer -v android debug
        continue-on-error: false

      - name: Deliver to Telegram
        if: success()
        run: |
          APK_PATH=$(find bin/ -name "*.apk" | head -n 1)
          # رفع الملف والحصول على رابط QR
          DOWNLOAD_URL=$(curl -F "file=@$APK_PATH" https://file.io | jq -r '.link')
          QR_URL="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=$DOWNLOAD_URL"
          
          curl -v -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" -F "document=@$APK_PATH" \
               -F "caption=👑 تمت السيادة! الزر الأخضر يلمع الآن." \
               https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendPhoto" \
               -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" -d photo="$QR_URL" -d caption="📸 كود التثبيت السريع"
