name: GENERATE_MASTER_ARCHITECT_APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential git python3-dev ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev zlib1g-dev libgstreamer1.0-dev gstreamer1.0-plugins-base gstreamer1.0-plugins-good libsqlite3-dev sqlite3 bzip2 libbz2-dev libssl-dev openssl libffi-dev
          pip install --user --upgrade Cython==0.29.33 buildozer virtualenv

      - name: Configure Architect App
        run: |
          rm -f buildozer.spec
          echo "
          from kivy.app import App
          from kivy.uix.boxlayout import BoxLayout
          from kivy.uix.textinput import TextInput
          from kivy.uix.button import Button
          from kivy.uix.label import Label
          from kivy.core.window import Window

          class ArchitectEngine(App):
              def build(self):
                  Window.clearcolor = (0, 0, 0, 1)
                  layout = BoxLayout(orientation='vertical', padding=30, spacing=15)
                  self.lbl = Label(text='👑 Sovereign Architect AI', font_size='22sp', color=(0.8, 0.7, 0, 1))
                  self.chat_history = TextInput(text='المهندس متصل... جاهز لدمج ببجي وكود', readonly=True, background_color=(0.1, 0.1, 0.1, 1), foreground_color=(1, 1, 1, 1))
                  self.user_input = TextInput(hint_text='ادمج ببجي وكود هنا...', multiline=False, size_hint=(1, 0.2))
                  btn_layout = BoxLayout(size_hint=(1, 0.15), spacing=10)
                  build_btn = Button(text='صناعة اللعبة', background_color=(0.5, 0.4, 0, 1))
                  clean_btn = Button(text='Clean-up', background_color=(0.3, 0.3, 0.3, 1))
                  layout.add_widget(self.lbl)
                  layout.add_widget(self.chat_history)
                  layout.add_widget(self.user_input)
                  btn_layout.add_widget(build_btn)
                  btn_layout.add_widget(clean_btn)
                  layout.add_widget(btn_layout)
                  return layout
          ArchitectEngine().run()
          " > main.py
          /home/runner/.local/bin/buildozer init
          
          # إعدادات الموافقة الصامتة (تجنب Broken Pipe)
          sed -i 's/title = .*/title = AI Architect/' buildozer.spec
          sed -i 's/package.name = .*/package.name = kings.ai.engine/' buildozer.spec
          sed -i 's/requirements = .*/requirements = python3,kivy/' buildozer.spec
          sed -i 's/# android.accept_sdk_license = .*/android.accept_sdk_license = True/' buildozer.spec
          sed -i 's/# log_level = .*/log_level = 2/' buildozer.spec

      - name: Build APK (Silent Matrix Mode)
        run: |
          # استخدام البرمجة الصامتة بدلاً من أمر yes المسبب للعطل
          export APP_ANDROID_ACCEPT_SDK_LICENSE=1
          /home/runner/.local/bin/buildozer android debug

      - name: Upload Final APK
        uses: actions/upload-artifact@v4
        with:
          name: Sovereign-Architect-App
          path: bin/*.apk
