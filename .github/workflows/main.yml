name: GENERATE_MASTER_ARCHITECT_APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04 # نسخة مستقرة لبناء الأندرويد
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Android Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip build-essential git python3-dev ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev zlib1g-dev libgstreamer1.0-dev gstreamer1.0-plugins-base gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-libav gstreamer1.0-doc gstreamer1.0-tools gstreamer1.0-x gstreamer1.0-alsa gstreamer1.0-gl gstreamer1.0-gtk3 gstreamer1.0-qt5 gstreamer1.0-pulseaudio libsqlite3-dev sqlite3 bzip2 libbz2-dev libssl-dev openssl libgdbm-dev libgdbm-compat-dev liblzma-dev libreadline-dev libncursesw5-dev libffi-dev uuid-dev libdb5.3-dev
          pip install --user --upgrade Cython==0.29.33 buildozer virtualenv

      - name: Build Architect Core and UI
        run: |
          rm -f buildozer.spec
          echo "
          from kivy.app import App
          from kivy.uix.boxlayout import BoxLayout
          from kivy.uix.textinput import TextInput
          from kivy.uix.button import Button
          from kivy.uix.label import Label
          from kivy.core.window import Window

          class ArchitectEngine(App):
              def build(self):
                  Window.clearcolor = (0, 0, 0, 1)
                  layout = BoxLayout(orientation='vertical', padding=30, spacing=15)
                  self.lbl = Label(text='👑 Sovereign Architect AI', font_size='22sp', color=(0.8, 0.7, 0, 1))
                  self.chat_history = TextInput(text='المهندس متصل الآن... اصف لي مشروعك القادم', readonly=True, background_color=(0.1, 0.1, 0.1, 1), foreground_color=(1, 1, 1, 1))
                  self.user_input = TextInput(hint_text='ادمج ببجي وكود هنا...', multiline=False, size_hint=(1, 0.2))
                  btn_layout = BoxLayout(size_hint=(1, 0.15), spacing=10)
                  build_btn = Button(text='صناعة اللعبة', background_color=(0.5, 0.4, 0, 1))
                  clean_btn = Button(text='Clean-up', background_color=(0.3, 0.3, 0.3, 1))
                  layout.add_widget(self.lbl)
                  layout.add_widget(self.chat_history)
                  layout.add_widget(self.user_input)
                  btn_layout.add_widget(build_btn)
                  btn_layout.add_widget(clean_btn)
                  layout.add_widget(btn_layout)
                  return layout
          ArchitectEngine().run()
          " > main.py
          /home/runner/.local/bin/buildozer init
          sed -i 's/title = .*/title = AI Architect/' buildozer.spec
          sed -i 's/package.name = .*/package.name = kings.ai.engine/' buildozer.spec
          sed -i 's/requirements = .*/requirements = python3,kivy/' buildozer.spec

      - name: Build APK (This will take 15-20 minutes)
        run: |
          yes | /home/runner/.local/bin/buildozer -v android debug

      - name: Upload Sovereign APK
        uses: actions/upload-artifact@v4
        with:
          name: Sovereign-Architect-App
          path: bin/*.apk
