name: GENERATE_MASTER_ARCHITECT_APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Build Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential git python3-dev ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev libportmidi-dev libswscale-dev libavformat-dev libavcodec-dev zlib1g-dev libgstreamer1.0-dev gstreamer1.0-plugins-base gstreamer1.0-plugins-good libsqlite3-dev sqlite3 bzip2 libbz2-dev libssl-dev openssl libffi-dev
          pip install --user --upgrade Cython==0.29.33 buildozer virtualenv

      - name: Create Architect AI Interface
        run: |
          rm -f buildozer.spec
          echo "
          from kivy.app import App
          from kivy.uix.boxlayout import BoxLayout
          from kivy.uix.textinput import TextInput
          from kivy.uix.button import Button
          from kivy.uix.label import Label
          from kivy.core.window import Window

          class ArchitectEngine(App):
              def build(self):
                  Window.clearcolor = (0.05, 0.05, 0.05, 1)
                  layout = BoxLayout(orientation='vertical', padding=20, spacing=10)
                  self.lbl = Label(text='👑 المبرمج الذكي - AI Architect', font_size='24sp', color=(1, 0.8, 0, 1))
                  self.chat = TextInput(text='أهلاً بك يا ملك. صف لي اللعبة التي تريد مني برمجتها الآن...', readonly=True, background_color=(0.1, 0.1, 0.1, 1), foreground_color=(1, 1, 1, 1))
                  self.user_input = TextInput(hint_text='مثال: ادمج ميكانيكا ببجي مع حركة كود...', multiline=True, size_hint_y=0.3)
                  btn = Button(text='بدء البرمجة والبناء', size_hint_y=0.2, background_color=(0, 0.5, 0.8, 1))
                  layout.add_widget(self.lbl)
                  layout.add_widget(self.chat)
                  layout.add_widget(self.user_input)
                  layout.add_widget(btn)
                  return layout
          ArchitectEngine().run()
          " > main.py
          /home/runner/.local/bin/buildozer init
          sed -i 's/title = .*/title = AI Architect/' buildozer.spec
          sed -i 's/package.name = .*/package.name = sovereign.ai.builder/' buildozer.spec
          sed -i 's/requirements = .*/requirements = python3,kivy/' buildozer.spec
          sed -i 's/# android.accept_sdk_license = .*/android.accept_sdk_license = True/' buildozer.spec

      - name: Physical APK Construction
        run: |
          export APP_ANDROID_ACCEPT_SDK_LICENSE=1
          # استخدام البناء الصامت لتجنب انقطاع البيانات
          /home/runner/.local/bin/buildozer android debug

      - name: Upload Sovereign APK
        uses: actions/upload-artifact@v4
        with:
          name: AI-Architect-Official-App
          path: bin/*.apk
