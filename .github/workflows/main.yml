      - name: Process and Send to Telegram
        run: |
          # 1. تثبيت الأدوات الناقصة (curl و jq) داخل السيرفر
          sudo apt-get update && sudo apt-get install -y curl jq
          
          # 2. العثور على ملف الـ APK
          APK_PATH=$(find bin/ -name "*.apk" | head -n 1)
          
          # 3. رفع الملف للحصول على رابط مباشر
          # ملاحظة: استخدمنا خدمة bashupload كبديل أسرع للروابط المباشرة
          DOWNLOAD_URL=$(curl -T "$APK_PATH" https://bashupload.com/$(basename "$APK_PATH") | grep -o 'https://bashupload.com/[^ ]*')
          
          # 4. توليد QR Code
          QR_URL="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=$DOWNLOAD_URL"
          
          # 5. إرسال الملف مباشرة إلى تليجرام
          curl -v -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" -F "document=@$APK_PATH" \
          -F "caption=👑 يا ملك، تم بناء المصنع بنجاح! 📦" \
          https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument

          # 6. إرسال الـ QR Code
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendPhoto" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d photo="$QR_URL" \
          -d caption="📸 مسح التثبيت السريع للنسخة السيادية"
