name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev curl jq
          pip install --user --upgrade buildozer cython virtualenv

      - name: Build with Buildozer
        env:
          # ربط المصنع بأدوات جيت هاب الجاهزة مباشرة
          ANDROID_HOME: /usr/local/lib/android/sdk
          ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
          ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/27.3.13750724
        run: |
          # تنظيف أي مخلفات سابقة ثم البناء
          buildozer -v android debug
        continue-on-error: false

      - name: Send to Telegram
        if: success()
        run: |
          APK_PATH=$(find bin/ -name "*.apk" | head -n 1)
          # رفع الملف والحصول على رابط للـ QR
          UPLOAD_DATA=$(curl -F "file=@$APK_PATH" https://file.io)
          DOWNLOAD_URL=$(echo $UPLOAD_DATA | jq -r '.link')
          QR_URL="https://api.qrserver.com/v1/create-qr-code/?size=300x300&data=$DOWNLOAD_URL"
          
          # إرسال الملف والـ QR
          curl -v -F "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" -F "document=@$APK_PATH" \
               -F "caption=👑 مبروك يا ملك! المصنع جاهز." \
               https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
          
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendPhoto" \
               -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" -d photo="$QR_URL" -d caption="📸 كود التثبيت"
