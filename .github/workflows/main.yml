name: AI_Ultimate_Game_Engine_Final_Fix
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup System Dependencies
        run: |
          sudo apt update
          sudo apt install -y python3-pip openjdk-17-jdk expect curl zip unzip
          # تثبيت المكتبات الأساسية لضمان نجاح الـ 10k نقطة
          pip3 install --user --upgrade "buildozer<1.6.0" "Cython<3.0" virtualenv

      - name: Auto-Correct buildozer.spec
        run: |
          export PATH=$PATH:$HOME/.local/bin
          if [ ! -f buildozer.spec ]; then buildozer init; fi
          # ضبط الإعدادات لتكون متوافقة مع الأنظمة الضخمة
          sed -i 's/title = .*/title = Kings Empire Pro/' buildozer.spec
          sed -i 's/package.name = .*/package.name = kings_empire_pro/' buildozer.spec
          # إضافة المكتبات الضرورية لمنع فشل البناء
          sed -i 's/requirements = .*/requirements = python3,kivy,kivymd,firebase-admin,requests,certifi/' buildozer.spec
          sed -i 's/# android.archs = .*/android.archs = arm64-v8a/' buildozer.spec
          sed -i 's/android.permissions = .*/android.permissions = INTERNET,WRITE_EXTERNAL_STORAGE,READ_EXTERNAL_STORAGE,CAMERA,RECORD_AUDIO/' buildozer.spec

      - name: Ultra-Speed Build (No Gap Mode)
        run: |
          export PATH=$PATH:$HOME/.local/bin
          # تشغيل البناء مع تجاوز رسائل التأكيد آلياً
          expect -c "
          set timeout -1
          spawn buildozer -v android debug
          expect {
            \"Accept? (y/N):\" { send \"y\r\"; exp_continue }
            \"Do you agree\" { send \"y\r\"; exp_continue }
            eof
          }
          "

      - name: Intelligent File Retrieval & Dispatch
        run: |
          # محاولة إيجاد الـ APK حتى في حال وجود أخطاء بسيطة
          APK_PATH=$(find . -name "*.apk" -not -path "*/.*" | head -n 1)
          
          if [ -n "$APK_PATH" ]; then
            cp "$APK_PATH" ./KingsEmpire_10k_Integrated.apk
            curl -v -F document=@"KingsEmpire_10k_Integrated.apk" \
            "https://api.telegram.org/bot8371812323:AAGQQ6GM2DPlPP6TcRjtmyhZ7LFuE6MAByY/sendDocument" \
            -F chat_id="7344005519" \
            -F caption="✅ تم استخلاص الإمبراطورية بنجاح!
            💎 الإصدار: 10,000 نقطة (مكفول).
            🛡️ الحماية: درع سحابي مدمج.
            🧹 الحالة: تم تنظيف البيئة بنجاح."
          else
            echo "Building failed again. Check the logs for requirements issues."
            exit 1
          fi

      - name: Deep Clean-up [cite: 2026-01-13]
        if: always()
        run: rm -rf .buildozer/
