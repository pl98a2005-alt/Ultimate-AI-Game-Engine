name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev openjdk-17-jdk wget
          pip install --user --upgrade "buildozer>=1.5.0" "Cython<3.0" virtualenv

      # 👑 حقن الأدوات يدوياً لتجنب خطأ "read of closed file"
      - name: Sovereign Toolchain Injection
        run: |
          # 1. إنشاء المجلدات المطلوبة
          PLATFORM_DIR="$HOME/.buildozer/android/platform"
          mkdir -p $PLATFORM_DIR
          
          # 2. تحميل الخط العربي
          curl -L -o Cairo-Bold.ttf https://github.com/google/fonts/raw/main/ofl/cairo/Cairo-Bold.ttf
          
          # 3. تحميل الـ NDK R25b يدوياً (wget أكثر استقراراً بمراحل)
          echo "Downloading NDK..."
          wget -q -c --tries=15 https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
          unzip -q android-ndk-r25b-linux.zip
          # نقله للمسار المتوقع بالضبط في الـ Log الخاص بك
          mv android-ndk-r25b $PLATFORM_DIR/android-ndk-r25.2.9519653
          
          # 4. تحميل الـ SDK Command Line Tools
          echo "Downloading SDK Tools..."
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          mkdir -p $PLATFORM_DIR/android-sdk/cmdline-tools
          unzip -q commandlinetools-linux-9477386_latest.zip -d $PLATFORM_DIR/android-sdk/cmdline-tools
          mv $PLATFORM_DIR/android-sdk/cmdline-tools/cmdline-tools $PLATFORM_DIR/android-sdk/cmdline-tools/latest

      - name: Build Execution
        run: |
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          export PATH=$PATH:$HOME/.local/bin
          
          # إجبار Buildozer على قبول التراخيص ورؤية المسارات المحقونة
          sed -i "s|^#\? \?android.accept_sdk_license =.*|android.accept_sdk_license = True|" buildozer.spec
          
          # تشغيل البناء! لن يحاول تحميل NDK الآن لأنه سيجده في مكانه
          buildozer -v android debug

      - name: Upload Final APK
        uses: actions/upload-artifact@v4
        with:
          name: Sovereign-Success-Build
          path: bin/*.apk
