name: GENERATE_MASTER_ARCHITECT_APK
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04 # النسخة الذهبية لاستقرار البناء
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install Essential Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential git python3-dev ffmpeg libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev zlib1g-dev libgstreamer1.0-dev libsqlite3-dev sqlite3 libssl-dev libffi-dev
          pip install --user --upgrade Cython==0.29.33 buildozer virtualenv

      - name: Inject AI Architect Brain
        run: |
          rm -f buildozer.spec
          # إنشاء جسد التطبيق (واجهة المبرمج)
          echo "
          from kivy.app import App
          from kivy.uix.label import Label
          from kivy.uix.boxlayout import BoxLayout
          from kivy.uix.textinput import TextInput
          from kivy.uix.button import Button

          class ArchitectApp(App):
              def build(self):
                  layout = BoxLayout(orientation='vertical', padding=10)
                  layout.add_widget(Label(text='👑 المبرمج الذكي: جاهز لصناعة ألعابك', font_size='20sp'))
                  self.input = TextInput(hint_text='ادمج ببجي وكود بـ 1100 تفصيل...', multiline=True)
                  layout.add_widget(self.input)
                  layout.add_widget(Button(text='بدء البرمجة والحفظ', size_hint_y=0.2))
                  return layout
          ArchitectApp().run()
          " > main.py
          
          # إعداد مواصفات الـ APK الملموس
          /home/runner/.local/bin/buildozer init
          sed -i 's/title = .*/title = AI Architect/' buildozer.spec
          sed -i 's/requirements = .*/requirements = python3,kivy/' buildozer.spec
          sed -i 's/# android.accept_sdk_license = .*/android.accept_sdk_license = True/' buildozer.spec

      - name: Build APK (The Real World Result)
        run: |
          # بناء التطبيق مع قبول التراخيص صامتاً
          export APP_ANDROID_ACCEPT_SDK_LICENSE=1
          /home/runner/.local/bin/buildozer -v android debug

      - name: Download Your APK
        uses: actions/upload-artifact@v4
        with:
          name: Sovereign-AI-Architect-Official
          path: bin/*.apk
