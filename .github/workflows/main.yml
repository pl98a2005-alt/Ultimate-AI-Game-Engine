name: Ultimate_AI_Game_Engine_Final_Fixed
on: [push]

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y python3-pip openjdk-17-jdk expect curl
          pip3 install --user --upgrade "buildozer<1.6.0" "Cython<3.0" virtualenv

      - name: Inject AI Brain & Analysis System
        run: |
          # بناء نظام الخبرة [cite: 2026-01-11] ونظام التنظيف [cite: 2026-01-13]
          echo "Initializing AI Brain for War of Kings..."
          echo "cleanup_extras = True" > engine_config.py

      - name: Build Optimized APK (arm64-v8a)
        run: |
          export PATH=$PATH:$HOME/.local/bin
          if [ ! -f buildozer.spec ]; then buildozer init; fi
          
          # إعدادات الهوية "حرب الملوك"
          sed -i 's/package.domain = org.test/package.domain = com.war.kings/' buildozer.spec
          sed -i 's/# android.archs = armeabi-v7a/android.archs = arm64-v8a/' buildozer.spec
          
          expect -c "
          set timeout -1
          spawn buildozer -v android debug
          expect {
            \"Accept? (y/N):\" { send \"y\r\"; exp_continue }
            \"Do you agree\" { send \"y\r\"; exp_continue }
            eof
          }
          "

      - name: Send APK to Telegram (Fixed)
        run: |
          # البحث عن الملف وإعادة تسميته لتجنب الخطأ 26
          ORIGINAL_APK=$(find bin/ -name "*.apk" | head -n 1)
          if [ -f "$ORIGINAL_APK" ]; then
            cp "$ORIGINAL_APK" ./WarOfKings.apk
            echo "File found and renamed. Sending to Telegram..."
            
            curl -F document=@"WarOfKings.apk" \
            "https://api.telegram.org/bot8371812323:AAGQQ6GM2DPlPP6TcRjtmyhZ7LFuE6MAByY/sendDocument" \
            -F chat_id="7344005519" \
            -F caption="✅ تم إصلاح العطل! نسخة 'حرب الملوك' جاهزة.
            
            🧠 التحليل: تم تجاوز خطأ القراءة (Code 26).
            🚀 ابدأ الآن بوصف ميكانيكا ببجي وكود داخل التطبيق."
          else
            echo "Error: APK not found in bin/ directory!"
            exit 1
          fi
