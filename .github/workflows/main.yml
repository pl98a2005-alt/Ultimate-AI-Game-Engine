name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Core Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev openjdk-17-jdk
          pip install --user --upgrade "buildozer>=1.5.0" cython virtualenv

      - name: Build Sovereign Environment
        run: |
          rm -rf .buildozer bin
          # 1. إنشاء مسار معزول تماماً بعيداً عن أعين GitHub الافتراضية
          export MY_SAFE_SDK=$HOME/sovereign_tools
          mkdir -p $MY_SAFE_SDK/cmdline-tools
          
          # 2. تحميل أدوات التحكم
          curl -L -o sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q sdk.zip -d $MY_SAFE_SDK/cmdline-tools
          mv $MY_SAFE_SDK/cmdline-tools/cmdline-tools $MY_SAFE_SDK/cmdline-tools/latest
          
          # 3. قبول التراخيص وحقن المسارات الكلاسيكية التي يطلبها Buildozer
          export PATH=$MY_SAFE_SDK/cmdline-tools/latest/bin:$PATH
          mkdir -p $MY_SAFE_SDK/tools/bin
          cp $MY_SAFE_SDK/cmdline-tools/latest/bin/sdkmanager $MY_SAFE_SDK/tools/bin/sdkmanager
          
          yes | sdkmanager --sdk_root=$MY_SAFE_SDK --licenses || true
          sdkmanager --sdk_root=$MY_SAFE_SDK "platforms;android-33" "build-tools;33.0.0" "ndk;25.2.9519653"

          # 4. تعديل الـ Spec بروابط مطلقة (Absolute Paths)
          sed -i "s|^#\? \?android.sdk_path =.*|android.sdk_path = $MY_SAFE_SDK|" buildozer.spec
          sed -i "s|^#\? \?android.ndk_path =.*|android.ndk_path = $MY_SAFE_SDK/ndk/25.2.9519653|" buildozer.spec
          sed -i 's|^#\? \?android.accept_sdk_license =.*|android.accept_sdk_license = True|' buildozer.spec

          # 5. 👑 التطهير النهائي لبيئة العمل
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          # مسح شامل لكل ما يمت بصلة لأندرويد في بيئة GitHub
          unset ANDROID_HOME
          unset ANDROID_SDK_ROOT
          unset ANDROID_NDK_HOME
          unset ANDROID_NDK_ROOT
          
          # تشغيل البناء باستخدام المسار المعزول
          /home/runner/.local/bin/buildozer -v android debug
