name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 👑 التحميل الفولاذي: سحب الخط والـ NDK يدوياً لضمان عدم الانقطاع
      - name: Pre-Download NDK & Arabic Fonts
        run: |
          # تحميل الخط العربي للقضاء على المربعات
          curl -L -o Cairo-Bold.ttf https://github.com/google/fonts/raw/main/ofl/cairo/Cairo-Bold.ttf
          
          # تحميل الـ NDK يدوياً بأداة wget المستقرة
          mkdir -p $HOME/ndk_manual
          wget -c -q --show-progress --tries=20 https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
          unzip -q android-ndk-r25b-linux.zip -d $HOME/ndk_manual
          echo "NDK_PATH_STATION=$HOME/ndk_manual/android-ndk-r25b" >> $GITHUB_ENV

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev openjdk-17-jdk
          pip install --user --upgrade "buildozer>=1.5.0" "Cython<3.0" virtualenv

      - name: Sovereign Build Execution
        run: |
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          export PATH=$PATH:$HOME/.local/bin
          
          # حقن المسار اليدوي للـ NDK في ملف الـ spec لتعطيل التحميل الآلي الفاشل
          sed -i "s|^#\? \?android.ndk_path =.*|android.ndk_path = ${{ env.NDK_PATH_STATION }}|" buildozer.spec
          sed -i "s|^#\? \?android.accept_sdk_license =.*|android.accept_sdk_license = True|" buildozer.spec
          sed -i "s|^#\? \?android.skip_update =.*|android.skip_update = True|" buildozer.spec
          
          # انطلاق البناء! لن يحاول تحميل NDK الآن
          buildozer -v android debug

      - name: Upload Final APK
        uses: actions/upload-artifact@v4
        with:
          name: Sovereign-Architect-APK
          path: bin/*.apk
