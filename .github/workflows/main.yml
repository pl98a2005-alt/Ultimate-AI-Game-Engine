name: SOVEREIGN_STATION_AUTO_BUILD

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Core
        run: |
          sudo apt-get update
          sudo apt-get install -y git zip unzip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo6 cmake libffi-dev libssl-dev openjdk-17-jdk
          pip install --user --upgrade "buildozer>=1.5.0" cython virtualenv

      - name: Forced SDK Injection
        run: |
          rm -rf .buildozer bin
          # 1. تجهيز المسار الملكي
          export SDK_DIR=$HOME/sovereign_tools
          mkdir -p $SDK_DIR/cmdline-tools
          curl -L -o sdk.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q sdk.zip -d $SDK_DIR/cmdline-tools
          mv $SDK_DIR/cmdline-tools/cmdline-tools $SDK_DIR/cmdline-tools/latest
          
          # 2. إنشاء الهيكل الذي يعشقه Buildozer قسراً
          mkdir -p $SDK_DIR/tools/bin
          cp $SDK_DIR/cmdline-tools/latest/bin/sdkmanager $SDK_DIR/tools/bin/sdkmanager
          chmod +x $SDK_DIR/tools/bin/sdkmanager
          
          # 3. قبول التراخيص وتثبيت الـ NDK 25b
          export PATH=$SDK_DIR/cmdline-tools/latest/bin:$PATH
          mkdir -p $SDK_DIR/licenses
          printf "8933bad161af4178b1185d1a37fbf41ea5269c55\nd56f5187479451eabf01fb7437c302a91464e303" > $SDK_DIR/licenses/android-sdk-license
          yes | sdkmanager --sdk_root=$SDK_DIR --licenses || true
          sdkmanager --sdk_root=$SDK_DIR "platforms;android-33" "build-tools;33.0.0" "ndk;25.2.9519653"

          # 4. 👑 الضربة القاضية: تعديل ملف spec بمسارات ثابتة ومنع التحديث
          sed -i "s|^#\? \?android.sdk_path =.*|android.sdk_path = $SDK_DIR|" buildozer.spec
          sed -i "s|^#\? \?android.ndk_path =.*|android.ndk_path = $SDK_DIR/ndk/25.2.9519653|" buildozer.spec
          sed -i "s|^#\? \?android.accept_sdk_license =.*|android.accept_sdk_license = True|" buildozer.spec
          # منع Buildozer من محاولة تحديث الـ SDK بنفسه
          sed -i "s|^#\? \?android.skip_update =.*|android.skip_update = True|" buildozer.spec

          # 5. تنظيف البيئة وتشغيل المحرك
          export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
          export ANDROID_HOME=$SDK_DIR
          export ANDROID_SDK_ROOT=$SDK_DIR
          # حذف أي أثر لـ NDK 27 من مسارات النظام
          sudo rm -rf /usr/local/lib/android/sdk/ndk/27* /home/runner/.local/bin/buildozer -v android debug
